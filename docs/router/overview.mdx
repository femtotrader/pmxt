---
title: What is the Router?
description: "One API key. Every prediction market. Cross-venue search, matching, price comparison, hedging, and arbitrage detection."
---

The Router is PMXT's cross-venue intelligence layer. One API key gives
you a unified view of every prediction market — Polymarket, Kalshi,
Limitless, and [8 more venues](/concepts/venues) — through a single
interface.

<Tabs>
  <Tab title="Python">
    ```python
    import pmxt

    router = pmxt.Router(pmxt_api_key="pmxt_live_...")

    # Search every venue at once
    markets = router.fetch_markets(query="fed rate cut", limit=5)

    # Find the same market on other venues
    matches = router.fetch_market_matches(market_id=markets[0].market_id)

    # Compare prices across venues
    prices = router.compare_market_prices(market_id=markets[0].market_id)
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import pmxt from "pmxtjs";

    const router = new pmxt.Router({ pmxtApiKey: "pmxt_live_..." });

    // Search every venue at once
    const markets = await router.fetchMarkets({ query: "fed rate cut", limit: 5 });

    // Find the same market on other venues
    const matches = await router.fetchMarketMatches({ marketId: markets[0].marketId });

    // Compare prices across venues
    const prices = await router.compareMarketPrices({ marketId: markets[0].marketId });
    ```
  </Tab>
</Tabs>

The Router only needs a PMXT API key — venue credentials are not
required for search, matching, or price analysis.

## What you can do

<CardGroup cols={2}>
  <Card title="Cross-venue search" icon="magnifying-glass" href="/router/events">
    Search markets and events across every venue in a single query.
    Results come from a shared catalog — ~10ms, not 11 sequential API
    calls.
  </Card>
  <Card title="Market matching" icon="link" href="/router/matching">
    Given a market on any venue, find the same or related market on
    every other venue. Each match has a relation type, confidence score,
    and reasoning.
  </Card>
  <Card title="Price comparison" icon="scale-balanced" href="/router/prices#comparemarketprices">
    Side-by-side bid/ask for the same market on every venue. See where
    it's cheapest to buy and where it pays the most to sell.
  </Card>
  <Card title="Hedge discovery" icon="shield-halved" href="/router/prices#fetchhedges">
    Find markets whose resolution conditions are subsets or supersets of
    your position — narrower or broader bets you can use to hedge.
  </Card>
  <Card title="Arbitrage detection" icon="arrow-right-arrow-left" href="/router/prices#fetcharbitrage">
    Scan for cross-venue price spreads. The Router identifies identity
    matches with divergent pricing, sorted by spread.
  </Card>
  <Card title="Compose with venues" icon="puzzle-piece" href="/router/prices#composing-with-venue-exchanges">
    Discover opportunities with the Router, then execute on venue
    exchanges. Same SDK, same schema — `marketId` works everywhere.
  </Card>
</CardGroup>

## How it works

The Router is backed by a continuously-updated Postgres catalog that
ingests markets, events, and outcomes from every venue PMXT supports.
When you search or match, you're querying this catalog — not fanning
out to 11 different APIs.

Matching uses embedding-based similarity and LLM-verified relation
classification to find semantically equivalent markets across venues,
even when venues use different titles, different bracket ranges, or
different framing.

| Venue A (Polymarket)              | Venue B (Kalshi)                    | Relation   |
| --------------------------------- | ----------------------------------- | ---------- |
| "Will BTC exceed $100k by Dec?"   | "Bitcoin above $100,000 on Dec 31"  | identity   |
| "Democrats win presidency"        | "Democratic nominee wins popular vote" | subset  |
| "Fed cuts rates in 2025"          | "Fed cuts rates in June"            | superset   |

## Get started

1. **Get an API key** at [pmxt.dev/dashboard](https://pmxt.dev/dashboard).
   It starts with `pmxt_live_` and works immediately.

2. **Install the SDK:**

<Tabs>
  <Tab title="Python">
    ```bash
    pip install pmxt
    ```
  </Tab>
  <Tab title="TypeScript">
    ```bash
    npm install pmxtjs
    ```
  </Tab>
</Tabs>

3. **Create a Router and start querying:**

<Tabs>
  <Tab title="Python">
    ```python
    import pmxt

    router = pmxt.Router(pmxt_api_key="pmxt_live_...")
    markets = router.fetch_markets(query="election")
    ```
  </Tab>
  <Tab title="TypeScript">
    ```typescript
    import pmxt from "pmxtjs";

    const router = new pmxt.Router({ pmxtApiKey: "pmxt_live_..." });
    const markets = await router.fetchMarkets({ query: "election" });
    ```
  </Tab>
</Tabs>

That's it. Everything the Router returns uses the same
[unified schema](/concepts/unified-schema) as every other PMXT
exchange, so you can pass a `marketId` from the Router straight into
a venue exchange when you're ready to trade.

<Info>
  **Order routing is coming.** Today the Router is read-only — it finds
  opportunities, but you execute trades on venue exchanges. Smart order
  routing (best-execution across venues) is on the roadmap.
</Info>
