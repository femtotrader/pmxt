{
    "name": "pmxt-monorepo",
    "private": true,
    "workspaces": [
        "core",
        "sdks/*"
    ],
    "scripts": {
        "server": "npm run server --workspace=pmxt-core",
        "generate": "npm run generate:sdk:all --workspace=pmxt-core",
        "test": "npm test --workspace=pmxt-core && ./scripts/verify-all.sh"
    }
}